*.iml
.gradle
/local.properties
/.idea
.DS_Store
/captures
.externalNativeBuild
.cxx
local.properties
app/build
build/

# signing (never commit)
interstellar.jks
keystore.properties
signing.properties

# 112MB sing-box core — rebuild via `make lib_android` (see README)
app/libs/libbox.aar

# sidecar cores (mihomo/xray/hev) — fetched via tools/fetch_cores.py (CI 缓存)
app/src/main/jniLibs/

# agent workspace / kotlin session files
.zcode/
.kotlin/

*.iml
.gradle
/local.properties
/.idea
.DS_Store
/captures
.externalNativeBuild
.cxx
local.properties
app/build
build/

# signing
#   仓库根的 signing.properties = CI 从 secrets 还原出来的临时文件，不入库
#   keystore/ 下的签名材料是**有意入库**的（零配置就能出正式签名包，见 keystore/README.md）
/signing.properties
interstellar.jks
keystore.properties

# 112MB sing-box core — rebuild via `make lib_android` (see README)
app/libs/libbox.aar

# sidecar cores (mihomo/xray/hev) — fetched via tools/fetch_cores.py (CI 缓存)
# 放在 full flavor 的 source set 下，slim flavor 因此天然不含它们
app/src/full/jniLibs/

# agent workspace / kotlin session files
.zcode/
.kotlin/

# python bytecode
__pycache__/
*.pyc
